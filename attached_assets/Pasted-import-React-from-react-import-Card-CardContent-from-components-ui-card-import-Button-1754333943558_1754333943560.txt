import React from "react";
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/ui/tabs";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";

export default function Dashboard() {
  return (
    <div className="flex min-h-screen flex-col bg-gray-50">
      <header className="sticky top-0 z-50 bg-white shadow px-6 py-4 flex items-center justify-between">
        <h1 className="text-xl font-bold">My IEP Hero</h1>
        <nav className="space-x-4">
          <Button variant="ghost">Dashboard</Button>
          <Button variant="ghost">Templates</Button>
          <Button variant="ghost">Chat</Button>
          <Button variant="ghost">Settings</Button>
        </nav>
      </header>

      <div className="flex flex-1">
        {/* Sidebar */}
        <aside className="w-64 bg-white border-r p-4 space-y-4">
          <div className="font-semibold text-gray-700">Parent Tools</div>
          <Button variant="outline" className="w-full">IEP Tracker</Button>
          <Button variant="outline" className="w-full">Document Vault</Button>
          <Button variant="outline" className="w-full">Letter Generator</Button>
          <div className="font-semibold text-gray-700 pt-4">Advocate Tools</div>
          <Button variant="outline" className="w-full">Case CRM</Button>
          <Button variant="outline" className="w-full">Scheduling</Button>
        </aside>

        {/* Main Panel */}
        <main className="flex-1 p-6 overflow-y-auto">
          <Tabs defaultValue="preview" className="w-full">
            <TabsList>
              <TabsTrigger value="preview">Live Preview</TabsTrigger>
              <TabsTrigger value="chat">Chat</TabsTrigger>
              <TabsTrigger value="notes">Notes</TabsTrigger>
            </TabsList>
            <TabsContent value="preview">
              <Card>
                <CardContent className="p-4">
                  <iframe
                    title="IEP Preview"
                    src="https://example.com/preview"
                    className="w-full h-[400px] border rounded"
                  ></iframe>
                </CardContent>
              </Card>
            </TabsContent>
            <TabsContent value="chat">
              <Textarea placeholder="Type your questions or updates here..." className="w-full" />
            </TabsContent>
            <TabsContent value="notes">
              <Input placeholder="Add meeting notes or to-dos" className="w-full" />
            </TabsContent>
          </Tabs>
        </main>
      </div>
    </div>
  );
}