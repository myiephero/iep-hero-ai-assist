run = "npm run start"
modules = ["nodejs-20"]

[env]
PORT = "5000"
NODE_ENV = "development"

[deployment]
run = "node deploy.js"
build = "npm run build"
deploymentTarget = "autoscale"

[deployment.env]
NODE_ENV = "production"
PORT = "5000"
REPLIT_DEPLOYMENT = "1"